深拷贝

```javascript
function deepClone(oirgin,target){
    var str = Object.prototype.toString();
    var tar = target || {};
    
    for(var k in origon){
        if(orgrin.hasOwnProperty(k)){
            if(typeof.origin[k] === 'object'&& origin[k]!==null){
                tar[k] = str.call(orgin[k]) === '[origin array]'?[]:{};
                deepClone(origin[k],tar[k]);
            }else{
                tar[k] = origin [k];
            }
        }
    }
    
    return tar;
}
```

